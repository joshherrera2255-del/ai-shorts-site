# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

`ai-shorts-site` is described as "AI Shorts ToS" — a site related to AI-generated short-form content, currently in its initial bootstrapped state. No framework, build system, or application code has been added yet.

## Current State

The repository has a single commit with only a `README.md`. There are no:
- Package manager files (`package.json`, `requirements.txt`, etc.)
- Build or test tooling
- Framework or language choices made

Before developing, the stack and tooling need to be established. Once they are, update this file with the relevant commands (build, lint, test, dev server).

## Conventions

- Branch for AI assistant work: `claude/claude-md-docs-pbivnl`
- Commits should be pushed to `origin claude/claude-md-docs-pbivnl` during development
